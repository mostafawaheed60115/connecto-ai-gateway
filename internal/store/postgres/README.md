# PostgreSQL store

The PostgreSQL repository boundary is defined in `internal/services`. The executable currently contains the compatibility implementation while the repository is being extracted; all SQL schema and persistence ownership belongs in this package.
